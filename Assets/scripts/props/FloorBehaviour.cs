using UnityEngine;

public class FloorBehaviour : MonoBehaviour {

	// Use this for initialization
	void Start () {
        RenderUtils.colorizeLightRed(GetComponent<Renderer>());
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
